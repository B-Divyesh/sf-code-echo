{
  "name": "code-echo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A local-first, code-aware reading aid for the browser.",
  "scripts": {
    "dev": "wxt",
    "dev:site": "vite --config site/vite.config.ts",
    "clean": "node scripts/clean.mjs",
    "build:extension": "wxt build && wxt zip",
    "optimize-assets": "node scripts/optimize-assets.mjs",
    "build:site": "npm run optimize-assets && npm run build:extension && vite build --config site/vite.config.ts && node scripts/stage-extension.mjs",
    "build": "npm run clean && npm run build:site",
    "prepare": "wxt prepare",
    "test": "npm run prepare && vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "typecheck": "npm run prepare && tsc --noEmit"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.10.2",
    "@playwright/test": "1.58.2",
    "@types/chrome": "^0.1.1",
    "@types/node": "^22.10.2",
    "sharp": "^0.35.4",
    "playwright-core": "1.58.2",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4",
    "wxt": "^0.21.4"
  }
}
